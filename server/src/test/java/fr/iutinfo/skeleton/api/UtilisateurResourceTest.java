package fr.iutinfo.skeleton.api;

import org.junit.Assert;
import org.junit.Test;


public class UtilisateurResourceTest {

	/*@Test
	public void should_find_user_by_token () {
		UserDao userDao = BDDFactory.getDbi().open(UserDao.class);
		userDao.dropUserTable();
		userDao.createUserTable();
		
		User belsa = new User();
		belsa.setLogin("belsa");
		String token = "123456789";
		belsa.setToken(token);
		userDao.insertUser(belsa);
		
		User user  = userDao.findByToken(token);
		Assert.assertEquals("belsa",user.getLogin());
	}*/
	
}
